# ZhiDu 知读 v0.6.0 — 安装与使用说明

这是一个 Obsidian 插件，基于《如何阅读一本书》的四层次方法论，在你的 Obsidian 里提供一个 AI 阅读陪伴代理。

- **Obsidian** = 一款免费的本地笔记软件（类似有道云笔记，但用纯文本写）
- **这个插件** = 装在 Obsidian 里的一个 AI 助手，专门陪你读书

下面的步骤完全按顺序做，每一步都不难，跟着做就行。

---

## 第一步（重要）：下载并安装 Obsidian

如果你电脑里还没装 Obsidian，先装它。已经装过的可以跳到第二步。

> ★ 官网下载页（推荐，会自动识别你的系统）：[obsidian.md/download](https://obsidian.md/download)

按系统直接选：

- **Windows 用户** —— 在上面页面点 "Download for Windows"，下一个 `.exe` 文件。下载完双击安装，全部点"下一步"即可。
- **Mac 用户** —— 在上面页面点 "Download for macOS"，下一个 `.dmg` 文件。下载完双击打开，把 Obsidian 图标拖进 Applications 文件夹。

★ 安装完成后第一次打开 Obsidian，会让你"创建新仓库"或"打开已有仓库"：

1. 点"创建新仓库（Create new vault）"
2. 给仓库起个名字（比如"我的读书笔记"）
3. 选一个文件夹位置保存它（比如电脑的"文档"目录里）
4. 点"创建"

> ★ **什么是"仓库（vault）"？**  
> 就是一个普通的电脑文件夹，Obsidian 把你所有的笔记都存在这里面。记住这个文件夹的位置，第三步要用到。

> ★ Obsidian 是免费的，不需要注册账号。  
> ★ 本插件只支持电脑版（Windows / Mac），手机和 iPad 上用不了。

---

## 第二步：准备一把 AI 大模型的 API Key

这个插件靠 AI 大模型工作，所以需要一把"钥匙"来调用 AI。

推荐用国内的"智谱 GLM"，注册简单、新用户有免费额度。

1. 打开网址：[bigmodel.cn](https://bigmodel.cn/)
2. 注册账号并登录（手机号即可）
3. 进入控制台，找到 **API Keys**（API 密钥）菜单 → 创建一个新的 Key
4. 把这串 Key 复制下来，先存到记事本里，第五步要用

如果你已经有 DeepSeek、OpenRouter、月之暗面等其他 Key 也可以用，在插件设置里换一下"服务商"就行。

---

## 第三步：把插件文件放到正确的位置

### (1) 找到你的 Obsidian 仓库文件夹

就是第一步里你创建仓库时选的那个文件夹。如果忘了在哪：

打开 Obsidian → 点左下角的齿轮（设置） → "关于" → 看到"仓库路径"，它就是你要找的文件夹。

或者：右键 Obsidian 左侧任意文件 → "在系统资源管理器中显示"，然后往上翻，翻到仓库根目录。

### (2) 进入仓库里的隐藏子目录

打开这个仓库文件夹后，里面应该有一个叫 `.obsidian` 的子文件夹（注意，名字最前面有一个点）。

> 【重要】这个文件夹可能是隐藏的，看不到的话：
> - **Windows 用户**：在资源管理器顶部的"查看"菜单里，勾选"显示" → "隐藏的项目"
> - **Mac 用户**：在 Finder 里按键盘快捷键 `Command + Shift + .` 切换显示

进入 `.obsidian` 文件夹后，找一个叫 `plugins` 的子文件夹。如果没有这个子文件夹，就自己新建一个，名字必须是小写的 `plugins`。

### (3) 把插件文件夹复制进去

把本仓库（或下载的 zip 解压后）里的 `zhidu-reader` 文件夹整个复制到刚才那个 `plugins` 目录里。

复制完成后，最终路径要长这样：

```
我的读书笔记/                        ← 你的仓库
└── .obsidian/
    └── plugins/
        └── zhidu-reader/            ← 就是这个位置
            ├── main.js
            ├── manifest.json
            └── styles.css
```

> ⚠️ **注意事项：**
> - 是把整个 `zhidu-reader` 文件夹复制进去，不是只复制里面的三个文件
> - 不要把本说明文件复制进去，它放在外面就好
> - 路径里不能多一层也不能少一层，必须正好是上面那个结构

---

## 第四步：在 Obsidian 里启用插件

1. 重启 Obsidian（关掉再重新打开一次）
2. 点左下角的齿轮（设置）
3. 在左侧菜单里点"第三方插件"（英文版叫 "Community plugins"）
4. 如果看到"安全模式"是打开的，点"关闭安全模式"（会弹一个确认框，点"确认关闭"即可）
5. 继续在这个页面往下拉，找到"已安装插件"区域，里面应该有一个 **ZhiDu 知读**
6. 点它右边的开关，让它变成打开状态

> ⚠️ **如果在"已安装插件"里找不到它：**
> - 检查第三步的路径是不是正好是 `.obsidian/plugins/zhidu-reader/main.js`
> - 三个文件（`main.js` / `manifest.json` / `styles.css`）是不是都在
> - 再彻底关掉 Obsidian 重新打开一次

---

## 第五步：填入 API Key

1. 还在"第三方插件"页面，在已安装插件列表里找到 "ZhiDu 知读"
2. 点它右边的齿轮图标（不是开关，是齿轮）
3. 进入插件的设置面板：
   - **服务商**：默认是"智谱 GLM"，不用改
   - **API Key**：把第二步里复制的那串 Key 粘贴进去
   - **主模型 / 摘要模型**：保持默认即可
4. 其他选项先都用默认值，以后熟悉了再调

---

## 第六步：开始使用

### 配套工具：PDF / EPUB 转 Markdown

本插件只认 `.md` 格式的书，如果手头是 PDF / EPUB，需要先转一下：

[mineru.net](https://mineru.net/) —— 上海人工智能实验室出的开源项目（MinerU）在线版，免费用。对中文书、扫描版、含公式/表格/图片的复杂版式识别效果都很好。

**用法**：打开网址 → 注册/登录 → 上传 PDF → 等转换完成 → 下载 md 文件。

### 1. 准备一本电子书

在 Obsidian 仓库的根目录下新建一个文件夹，命名为 `book-library`，把你想读的书（Markdown 格式的 `.md` 文件）放进去。比如：

```
我的读书笔记/book-library/如何阅读一本书.md
```

完整流程：

1. 准备好一本 PDF 或 EPUB 电子书
2. 到 [mineru.net](https://mineru.net/) 上传这个文件，下载转换后的 md
3. 把这个 md 文件放到仓库的 `book-library` 文件夹里
4. 完成，去 Obsidian 里打开插件聊天面板开始用

> ⚠️ 本插件只认 `.md` 格式的书，PDF / EPUB / MOBI 都要先转 md

### 2. 打开聊天面板

在 Obsidian 左侧工具栏里，找到一个"书本"形状的图标，点它。右侧会弹出 ZhiDu 的聊天面板。

### 3. 开始和 AI 对话

在输入框里直接键入一个 `/` 斜杠，就会弹出所有可用命令。或者直接提问也行。

常用命令（推荐按这个顺序用）：

| 命令 | 说明 |
|------|------|
| `/inspect 《书名》` | 检视阅读（10 分钟看懂全书结构） |
| `/analyze 《书名》` | 分析阅读（逐章深读，提炼论证和概念） |
| `/theme 话题` | 主题阅读（多本书围绕同一话题横向对比） |
| `/ask-four 《书名》` | 回答艾德勒的四个基本问题 |
| `/help` | 查看完整命令列表 |

### 4. 笔记会自动生成到哪里？

插件会在仓库里自动创建一个 `知读笔记` 文件夹，所有笔记都在这里，包括结构笔记、概念笔记、辩证笔记等。

---

## 常见问题

### Q1. 启用后看不到右侧的聊天面板？

- 看左侧工具栏里有没有一个"书本"图标，点一下
- 或者按 `Ctrl+P`（Mac 是 `Cmd+P`） → 搜索 "ZhiDu" → 选择打开面板的命令

### Q2. 报错 "API Key 无效" 或 "请求失败"？

- 检查 Key 是不是复制完整，前后没有多余的空格
- 登录服务商控制台看看账户是否有余额
- 确认 Key 对应的账户已经开通了所选模型的访问权限

### Q3. 我有多个仓库（vault），每个都要装一次吗？

- 是的，每个仓库要独立装一次
- 直接把 `zhidu-reader` 文件夹复制到每个仓库的 `.obsidian/plugins/` 下即可

### Q4. 我的仓库用网盘或 git 同步，API Key 会被一起上传吗？

- 会。API Key 保存在 `.obsidian/plugins/zhidu-reader/data.json` 里
- 如果用的是 iCloud / OneDrive / Dropbox 之类的网盘同步，文件会传到云端，不建议在公共电脑上用同一个仓库
- 如果用 git，请在 `.gitignore` 里加一行：
  ```
  .obsidian/plugins/zhidu-reader/data.json
  ```

### Q5. 怎么升级到新版本？

- 把新版的三个文件（`main.js` / `manifest.json` / `styles.css`）覆盖到 `仓库/.obsidian/plugins/zhidu-reader/` 目录
- 然后按 `Ctrl+P`（Mac 是 `Cmd+P`） → 输入 "Reload app" → 回车即可

### Q6. 主题阅读 `/theme` 会很贵吗？

- 默认最多并发读 10 本书，每本都会调用一次 AI 做摘要
- 如果担心成本，在插件设置里调低这两项：
  - "每个会话的联网搜索上限"
  - "摘要模型的并发数"

---

## 如何卸载

1. Obsidian 里：设置 → 第三方插件 → 关掉 "ZhiDu 知读" 的开关
2. 把 `仓库/.obsidian/plugins/zhidu-reader/` 整个文件夹删掉

连同会话记录和设置也会一起清除（因为都存在这个文件夹里）。

---

## 协议

[MIT License](LICENSE)

有任何问题，欢迎在本仓库 [Issues](../../issues) 区提问。
